{
  "name": "leon-portfolio-backend",
  "version": "1.0.0",
  "description": "Dependencies for the Vercel serverless function.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/genai": "^0.15.0"
  }
}
